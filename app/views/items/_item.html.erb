<ul>
    <a href="" class="list-group-item <% unless item.read != false %>text-info<% end %> list-group-item-action flex-column align-items-start">
      <div class="d-flex w-100 justify-content-between">
        <h5 class="mb-1"><%= item.title %></h5>
        <small><%= item.pub_date.strftime('%d/%m/%Y') %></small>
      </div>
      <p class="mb-1"><%= strip_tags(item.description.truncate(150)) %></p>
      <small><a href="?">Marquer comme lu</a></small>
    </a>

</ul>
